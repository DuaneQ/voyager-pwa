import { useNavigate } from "react-router-dom";

export const Search = () => {
  const navigate = useNavigate();

  return (
    <div className="authFormContainer">
      <p>Search</p>
    </div>
  );
};
