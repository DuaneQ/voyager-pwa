# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local

# Environment variable files — contain secrets, never commit
.env
.env.*
!.env.example

# Firebase CLI debug logs — may contain secrets printed during deployment
firebase-debug.log
firebase-debug.*.log
